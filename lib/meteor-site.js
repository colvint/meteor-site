// organization-level roles (admin, member) should be managed automatically
// group type (e.g. school group) roles should be managed in the role manager

MeteorSite = {
  roles: ['admin', 'member', 'content-manager']
};
